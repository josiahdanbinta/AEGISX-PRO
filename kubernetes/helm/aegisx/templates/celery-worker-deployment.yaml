apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "AEGIS.fullname" . }}-celery-worker
  labels:
    {{- include "AEGIS.labels" . | nindent 4 }}
    app.kubernetes.io/component: celery-worker
spec:
  replicas: {{ .Values.celeryWorker.replicaCount }}
  selector:
    matchLabels:
      {{- include "AEGIS.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: celery-worker
  template:
    metadata:
      labels:
        {{- include "AEGIS.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: celery-worker
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: celery-worker
          image: {{ include "AEGIS.image" (dict "imageRoot" .Values.celeryWorker.image "Values" .Values) }}
          imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
          command: ["celery"]
          args: ["-A", "AEGIS", "worker", "-l", {{ .Values.config.logLevel | lower | quote }}]
          envFrom:
            - configMapRef:
                name: {{ include "AEGIS.fullname" . }}-config
            - secretRef:
                name: {{ include "AEGIS.fullname" . }}-secrets
          resources:
            {{- toYaml .Values.celeryWorker.resources | nindent 12 }}
          securityContext:
            runAsNonRoot: true
            runAsUser: {{ .Values.global.securityContext.runAsUser }}
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
