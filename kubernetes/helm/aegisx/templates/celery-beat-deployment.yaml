apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "AEGIS.fullname" . }}-celery-beat
  labels:
    {{- include "AEGIS.labels" . | nindent 4 }}
    app.kubernetes.io/component: celery-beat
spec:
  replicas: {{ .Values.celeryBeat.replicaCount }}
  selector:
    matchLabels:
      {{- include "AEGIS.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: celery-beat
  template:
    metadata:
      labels:
        {{- include "AEGIS.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: celery-beat
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: celery-beat
          image: {{ include "AEGIS.image" (dict "imageRoot" .Values.backend.image "Values" .Values) }}
          imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
          command: ["celery"]
          args: ["-A", "AEGIS", "beat", "-l", {{ .Values.config.logLevel | lower | quote }}]
          envFrom:
            - configMapRef:
                name: {{ include "AEGIS.fullname" . }}-config
            - secretRef:
                name: {{ include "AEGIS.fullname" . }}-secrets
          resources:
            {{- toYaml .Values.celeryBeat.resources | nindent 12 }}
          securityContext:
            runAsNonRoot: true
            runAsUser: {{ .Values.global.securityContext.runAsUser }}
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
